l[m,k,0]

train composition
m=0 -> idx: [2,3] -> 1 train -> [0,0]
m=1 -> idx: [6,7] -> 1 train -> [0,0]
m=2 -> idx: [10,11] -> 1 train -> [0,0]

what happens to the other 6 binary variables????

m=0 -> idx[0,1] -> xi[m,k,link[m,k,0]] and xi[m,k,link[m,k,1]] -> no waiting -> ?

xi.shape = (num_line, N, N+1)

xi=0 -> trains not available


-why do we compute a new time schedule if we only need the first departure time?
